// useEffect - handle side Effects

// Side effects - functions that affect other components or cannot be done during rendering
// 1. Fetching data from an API
// 2. Subscribing to a data stream
// 3. Setting up timers
// 4. Directly manipulating the DOM

// Trivia-API -> answers -> Trivia-API


// Trivia-API -> isComplete(boolean) -> answers all the qs -> Trivia-API

document.addEventListener('DOMContentLoaded', () => {
  fetch
})


useEffect(fn()) -> // Infinite
useEffect(fn(), []) -> // Runs once - there is no state to sync with
useEffect(fn(), [name]) -> // Runs once - but sync with the name everytime it changes

